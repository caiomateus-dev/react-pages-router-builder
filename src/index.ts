export { router } from "./router";
export type { ExtendedRouteObject, FileSystemRouterOptions } from "./types";

// Re-export React Router types for convenience
export type { RouteObject } from "react-router";
